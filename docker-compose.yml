version: "3.8"
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
      - JWT_SECRET=change_this_to_a_long_random_secret
      - DB_FILE=/app/data/db.sqlite
